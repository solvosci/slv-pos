* If more fields inherited from Sales Order are still missing, add here.
* `sale_stock` inheritance is not directly mentioned, but it's required. It
  seems to be unnecessary (since `stock` and `sale` are automatically
  installed), but if problems are detected, we should add it.
